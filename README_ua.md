# Telegram-боти відключень електроенергії

Незалежні Telegram-боти для відстеження планових відключень електроенергії від різних постачальників в Україні.

## Огляд

Цей проєкт надає **незалежні боти для кожного постачальника** для перевірки графіків відключень. Кожен постачальник (ДТЕК, ЦЕК) має власного автономного бота, який можна розгорнути окремо для різних клієнтів.

### Підтримувані постачальники

- **ДТЕК** (DTEK) - Обслуговує Дніпро, Київ, Одесу та інші регіони
- **ЦЕК** (CEK) - Центральна Енергетична Компанія

## Архітектура

Проєкт використовує **багатоботову архітектуру** зі спільною логікою:

```
shutdowns/
├── common/                 # Спільна бібліотека (~70% логіки)
│   ├── bot_base.py         # База даних, FSM, утиліти, CAPTCHA
│   ├── handlers.py         # Усі обробники команд/callback
│   ├── tasks.py            # Фонові завдання (підписки, сповіщення)
│   ├── data_source.py      # Абстрактний інтерфейс джерела даних
│   ├── formatting.py       # Форматування тексту графіків
│   └── visualization.py    # Генерація зображень графіків
│
├── dtek/                   # Постачальник ДТЕК (~400 рядків)
│   ├── parser/             # Веб-скрапер ДТЕК
│   ├── data_source.py      # Реалізація джерела даних ДТЕК
│   └── bot/                # Тонкі обгортки + конфігурація
│       ├── bot.py
│       └── docker-compose.yml
│
├── cek/                    # Постачальник ЦЕК (~400 рядків)  
│   ├── parser/             # Веб-скрапер ЦЕК
│   ├── data_source.py      # Реалізація джерела даних ЦЕК
│   └── bot/                # Тонкі обгортки + конфігурація
│       ├── bot.py
│       └── docker-compose.yml
│
└── resources/              # Спільні шрифти та ресурси
```

### Ключові функції

✅ **Незалежне розгортання** - Кожен бот можна розгорнути окремо  
✅ **Спільна логіка** - Загальна бібліотека усуває дублювання коду  
✅ **Оптимізація для постачальника** - Кожен бот оптимізований для свого постачальника  
✅ **Легке масштабування** - Створення кількох екземплярів для різних клієнтів  
✅ **Без API-шару** - Боти викликають парсери безпосередньо для кращої продуктивності

## Швидкий старт

### Розгортання ДТЕК-бота

```bash
cd dtek/bot
cp .env.example .env
# Відредагуйте .env та додайте DTEK_BOT_TOKEN
docker-compose up -d
```

Див. [dtek/bot/README.md](dtek/bot/README.md) для детальних інструкцій.

### Розгортання ЦЕК-бота

```bash
cd cek/bot
cp .env.example .env
# Відредагуйте .env та додайте CEK_BOT_TOKEN
docker-compose up -d
```

Див. [cek/bot/README.md](cek/bot/README.md) для детальних інструкцій.

## Функції ботів

Обидва боти підтримують:

- 🔍 **Пошук адреси** - Перевірка графіків відключень за адресою
- 📖 **Адресна книга** - Збереження кількох адрес для швидкого доступу (команда `/addresses`)
- 📊 **Візуальні діаграми** - Круглий годинник з трикутним маркером години
- 🔔 **Мультипідписки** - Підписка на кілька адрес одночас��о
- ⚠️ **Сповіщення** - Повідомлення за N хвилин до подій
- 🤖 **Захист CAPTCHA** - Захист бота від ботів
- 💾 **Локальна база даних** - SQLite з підтримкою міграцій
- 📈 **Статистика** - Команда `/stats` тільки для адміністраторів
- ⚡ **Кешування по групах** - Інтелектуальне кешування зменшує навантаження на постачальників на 90-99%
- 🗄️ **Нормалізована база даних** - Ефективне зберігання адрес усуває дублювання

### Функції залежно від постачальника

| Функція | ДТЕК | ЦЕК |
|---------|------|-----|
| Візуалізація | 48 годин (2 дні) | 24 години (сьогодні) |
| Кешування по групах | Так (TTL 15 хв) | Так (TTL 15 хв) |
| Відображення графіку | Усі дні | Тільки сьогодні |
| Продуктивність кешу | < 1 сек (HIT) | < 1 сек (HIT) |

## Розгорнуті боти
Користувачі можуть підписатися на оновлення та взаємодіяти з ботами безпосередньо в Telegram.
Боти вже розгорнуті, працюють у Telegram і доступні за такими іменами:
- **ДТЕК**: `@dtek_disconnections_bot`
- **ЦЕК**: `@cek_disconnections_bot`

## Оптимізація продуктивності

### Кешування по групах ⚡

Розумна система кешування, яка значно зменшує навантаження на постачальників та покращує час відповіді:

- **Принцип роботи**: Адреси групуються за групами постачальника (напр., "3.1", "3.2")
- **Перша перевірка**: Викликає парсер (5-15 сек) → Зберігає результат у кеші групи
- **Наступні перевірки**: Використовує кеш, якщо та ж група (< 1 сек)
- **TTL кешу**: 15 хвилин (гарантовані свіжі дані)
- **Вплив**: Зменшення викликів парсера на 90-99%

**Приклад**: Користувач перевіряє 3 адреси з групи "3.1":
- Без кешування: 3 виклики парсера (15-45 сек загалом)
- З кешуванням: 1 виклик парсера + 2 попадання в кеш (< 7 сек загалом)

### Нормалізація бази даних 🗄️

Ефективне зберігання адрес усуває дублювання:

- **Центральна таблиця `addresses`**: Єдине джерело правди для всіх даних адрес
- **Посилання зовнішніх ключів**: `subscriptions` та `user_last_check` використовують `address_id`
- **Економія місця**: ~75% зменшення для користувачів з кількома підписками
- **Узгодженість даних**: Зміни адрес поширюються автоматично
- **Продуктивність запитів**: JOIN по INTEGER замість TEXT

### Статус міграції 006

Версія бази даних 6 включає обидві оптимізації. Автоматична міграція при запуску бота.

## Спільна бібліотека

Директорія `common/` містить спільну логіку, яку використовують обидва боти:

- **`bot_base.py`** (~1180 рядків)
  - Підключення до бази даних (SQLite)
  - Стани FSM для взаємодії з користувачем
  - Логіка CAPTCHA
  - Парсинг адрес та CRUD адресної книги
  - Хешування графіків для виявлення змін
  - Управління мультипідписками
  - **Управління кешем груп** (get/update/invalidate)
  - **Операції з нормалізованими адресами** (get_address_id, update_address_group)

- **`handlers.py`** (~1250 рядків)
  - Усі обробники команд (start, check, subscribe, repeat, stats тощо)
  - Обробники callback для вибору адрес
  - Форматування та надсилання відповідей
  - Параметризовано для специфічних налаштувань постачальника через `BotContext`
  - **Інтегроване кешування груп** у перевірках адрес

- **`tasks.py`** (~590 рядків)
  - Фонові завдання (subscription_checker, alert_checker)
  - Логіка обробки сповіщень
  - **Оптимізація кешу груп** у перевірках підписок

- **`migrate.py`** - CLI міграції бази даних
  - Міграції схеми з відстеженням версій
  - Запуск перед стартом бота для нових схем

- **`formatting.py`** (~350 рядків)
  - Форматування тексту графіків
  - Повідомлення про поточний статус
  - Об'єднання часових слотів

- **`visualization.py`** (~800 рядків)
  - Обертові кругові діаграми (48год та 24год)
  - Трикутний маркер години, що вказує вгору
  - Розумне позиціонування дати в центрі
  - Генерація зображень PIL/Pillow

## Джерела даних (підключувальна архітектура)

Проєкт підтримує **підключувальну архітектуру джерел даних**, що дозволяє постачальникам перемикатися між різними методами отримання даних (Веб-парсер, База даних, API) без зміни основної логіки бота.

- **Інтерфейс**: Визначено в `common/data_source.py` (`ShutdownDataSource`).
- **Реалізація**: Кожен постачальник реалізує власне джерело даних (напр., `dtek/data_source.py`).
- **Конфігурація**: Керується через змінну середовища `DATA_SOURCE_TYPE`.

Детальний посібник з реалізації див. у [DATA_SOURCES.md](DATA_SOURCES.md).

## Розробка

### Версія Python

Цей проєкт вимагає **Python 3.13 або 3.14** (рекомендовано: 3.14.1).

Версія Python керується через:
- `.python-version` - Використовується pyenv та іншими менеджерами версій
- `pyproject.toml` - Вказує `requires-python = ">=3.13,<3.15"`

### Швидке налаштування

**Автоматизоване налаштування (рекомендовано):**
```bash
# Створити conda-середовище
conda create -n shutdowns python=3.14
conda activate shutdowns

# Запустити автоматизований скрипт налаштування
./setup_dev.sh
```

**Ручне налаштування:**
```bash
# Створити віртуальне середовище
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Встановити залежності
pip install -r requirements-dev.txt
```

### Переваги структури проєкту

1. **Принцип DRY** - Спільна логіка в одному місці
2. **Незалежність** - Кожен постачальник самодостатній
3. **Масштабованість** - Легко додавати нових постачальників
4. **Супроводжуваність** - Виправлення помилок застосовуються до всіх ботів
5. **Клієнтоорієнтованість** - Розгортання окремих екземплярів для кожного клієнта

### Міграції бази даних

Перед першим запуском ботів (або після змін схеми), застосуйте міграції:

```bash
# Застосувати міграції до бази даних
python -m common.migrate --db-path ./data/bot.db

# Перевірити статус міграції
python -m common.migrate --db-path ./data/bot.db --status
```

### Локальний запуск

```bash
# Запустити ДТЕК-бота
export DTEK_BOT_TOKEN="ваш_токен"
export DTEK_DB_PATH="./dtek_bot.db"
python -m common.migrate --db-path ./dtek_bot.db  # Тільки перший раз
python -m dtek.bot.bot

# Запустити ЦЕК-бота
export CEK_BOT_TOKEN="ваш_токен"
export CEK_DB_PATH="./cek_bot.db"
python -m common.migrate --db-path ./cek_bot.db  # Тільки перший раз
python -m cek.bot.bot
```

### Тестування

```bash
# Запустити всі тести для всіх компонентів
./run_tests.sh

# Запустити тести для конкретного постачальника
./run_tests.sh all dtek
./run_tests.sh all cek

# Запустити конкретні типи тестів
./run_tests.sh unit all      # Тільки юніт-тести
./run_tests.sh coverage all  # З звітом про покриття
./run_tests.sh quick all     # Пропустити повільні тести

# Або використати pytest безпосередньо
pytest                        # Усі тести
pytest dtek/tests/           # Тільки тести ДТЕК
pytest cek/tests/            # Тільки тести ЦЕК
pytest common/tests/         # Тести спільної бібліотеки
```

## Технічний стек

- **Python**: 3.14 (сумісний з 3.13-3.14)
- **Фреймворк бота**: aiogram 3.x
- **База даних**: SQLite (aiosqlite)
- **Веб-скрапінг**: Botasaurus (безголовий Chrome)
- **Генерація зображень**: Pillow (PIL)
- **Розгортання**: Docker + Docker Compose

## Міграція зі старої архітектури

Цей проєкт був рефакторизований з централізованої архітектури (один бот + API) на незалежні боти для кожного постачальника. Переваги:

- ✅ Не потрібен API-шар (боти викликають парсери безпосередньо)
- ✅ Простіше розгортання (один Docker-контейнер на бота)
- ✅ Краща ізоляція (ДТЕК і ЦЕК незалежні)
- ✅ Легше налаштувати під клієнта
- ✅ Спільна логіка через загальну бібліотеку (DRY)

## Внесок у проєкт

При додаванні нових функцій:

1. **Спільна логіка** → Додати до `common/`
2. **Специфічне для постачальника** → Додати до `provider/bot/` або `provider/parser/`
3. **Тести** → Додати до `provider/tests/` або `tests/test_common/`

## Ліцензія

MIT

## Підтримка

З питань або проблем:
- Перевірте README для конкретного постачальника: [dtek/bot/README.md](dtek/bot/README.md), [cek/bot/README.md](cek/bot/README.md)
- Перегляньте логи: `docker-compose logs -f <ім'я_бота>`
- Відкрийте issue на GitHub
