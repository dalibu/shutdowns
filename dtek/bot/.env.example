# DTEK Bot Environment Variables
# Copy this file to .env and fill in your values

# Telegram Bot Token (required)
# Get it from @BotFather on Telegram
BOT_TOKEN=your_bot_token_here

# Database path (optional, default: /data/bot.db)
DB_PATH=/data/bot.db

# Font path (optional, default: /app/resources/DejaVuSans.ttf)
FONT_PATH=/app/resources/DejaVuSans.ttf


# Logging level (optional, default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log directory inside container (optional, default: /logs)
LOG_DIR=/logs

# Admin user IDs (comma-separated, for /stats command)
ADMIN_IDS=123456789

# --- Caching and Subscription Settings (optional) ---

# Group cache TTL in minutes (default: 15)
# How long to keep group schedule cache before refreshing
# GROUP_CACHE_TTL_MINUTES=15

# Subscription checker interval in seconds (default: 300 = 5 minutes)
# How often to check for subscription updates
# CHECKER_LOOP_INTERVAL_SECONDS=300

# Default subscription interval in hours (default: 1.0)
# Default frequency for new subscriptions
# DEFAULT_INTERVAL_HOURS=1.0
